Place the official bank logo in this folder.

Recommended file path:

- `src/assets/branding/bank-logo.svg`

If you already have a PNG version, replace the placeholder and keep the same visual proportions, or update the `logoPath` value in:

- `src/app/app.component.ts`
